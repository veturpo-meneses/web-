<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Countries List - Countries Info</title>
    <link href="/css/output.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
        }
    </style>
</head>
<body class="bg-gray-100">
    <!-- Navbar Partial -->
    <%- include('partials/navbar', { page: 'Countries' }) %>

    <div class="container mx-auto my-16">
        <h1 class="text-4xl font-bold text-center text-gray-800 mb-8">Countries Information</h1>
        <p class="text-lg text-center text-gray-700 mb-12">Browse through a list of countries with detailed information.</p>

        <!-- Countries Table -->
        <div class="overflow-x-auto">
            <table class="min-w-full bg-white shadow-md rounded-lg">
                <thead>
                    <tr class="bg-blue-600 text-white">
                        <th class="p-4">Flag</th>
                        <th class="p-4">Name</th>
                        <th class="p-4">Region</th>
                        <th class="p-4">Subregion</th>
                        <th class="p-4">Capital</th>
                        <th class="p-4">Population</th>
                        <th class="p-4">Area (sq km)</th>
                        <th class="p-4">Details</th>
                    </tr>
                </thead>
                <tbody>
                    <% countries.forEach(country => { %>
                        <tr class="border-b hover:bg-gray-100 transition duration-300">
                            <!-- Country Flag -->
                            <td class="p-4 text-center">
                                <img src="<%= country.flag %>" alt="Flag of <%= country.name %>" class="h-10 w-16 object-cover rounded">
                            </td>

                            <!-- Country Name -->
                            <td class="p-4 font-semibold">
                                <%= country.commonName %> 
                                <% if (country.commonName !== country.nativeName) { %>
                                    (<%= country.nativeName %>)
                                <% } %>
                            </td>

                            <!-- Region -->
                            <td class="p-4 text-center">
                                <a href="/region/<%= country.region %>" class="text-blue-600 hover:underline">
                                    <%= country.region %>
                                </a>
                            </td>

                            <!-- Subregion -->
                            <td class="p-4 text-center">
                                <a href="/subregion/<%= country.subRegion %>" class="text-blue-600 hover:underline">
                                    <%= country.subRegion %>
                                </a>
                            </td>

                            <!-- Capital -->
                            <td class="p-4 text-center">
                                <%= country.capital %>
                            </td>

                            <!-- Population -->
                            <td class="p-4 text-center">
                                <%= country.population.toLocaleString() %>
                            </td>

                            <!-- Area -->
                            <td class="p-4 text-center">
                                <%= country.area ? country.area.toLocaleString() : 'N/A' %>
                            </td>

                            <!-- Details Link -->
                            <td class="p-4 text-center">
                                <a href="/countries/<%= country.id %>" class
::contentReference[oaicite:0]{index=0}
 


